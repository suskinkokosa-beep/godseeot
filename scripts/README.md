# Скрипты Isleborn Online

Эта директория содержит скрипты для автоматизации установки и запуска проекта.

## 📋 Список скриптов

### Установка

#### Ubuntu/Linux
- **`install_ubuntu.sh`** - Автоматическая установка всех зависимостей для Ubuntu 20.04+
  - Устанавливает Godot 4.2
  - Устанавливает Docker и Docker Compose
  - Устанавливает PostgreSQL, Redis
  - Устанавливает Python, Go
  - Настраивает базу данных
  - Настраивает Island Service

#### Windows
- **`install_windows.ps1`** - Автоматическая установка для Windows 10+
  - Требует PowerShell от имени администратора
  - Устанавливает Chocolatey (если отсутствует)
  - Устанавливает все необходимые зависимости
  - Настраивает базу данных

### Запуск

#### Ubuntu/Linux
- **`run_all.sh`** - Запускает все сервисы:
  - PostgreSQL, Redis, Nakama (Docker)
  - Island Service (Python)
  - Gateway (Go)
  - Godot Server (headless)

#### Windows
- **`run_all.ps1`** - Запускает все сервисы (аналог для Windows)

### Остановка

#### Ubuntu/Linux
- **`stop_all.sh`** - Останавливает все запущенные сервисы

#### Windows
- **`stop_all.ps1`** - Останавливает все запущенные сервисы

## 🚀 Использование

### Первый запуск (Ubuntu)

```bash
# 1. Сделайте скрипты исполняемыми
chmod +x scripts/*.sh

# 2. Запустите установку
./scripts/install_ubuntu.sh

# 3. Перелогиньтесь или выполните:
newgrp docker

# 4. Запустите все сервисы
./scripts/run_all.sh
```

### Первый запуск (Windows)

```powershell
# 1. Разрешите выполнение скриптов
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 2. Запустите установку (от имени администратора)
.\scripts\install_windows.ps1

# 3. Запустите все сервисы
.\scripts\run_all.ps1
```

## 📝 Примечания

- Все скрипты можно запускать многократно (идемпотентны)
- Скрипты проверяют наличие установленных компонентов перед установкой
- Логи сохраняются в директории `logs/`
- PID файлы сохраняются для корректной остановки сервисов

## 🔧 Ручной запуск отдельных сервисов

Если нужно запустить сервисы вручную, см. [INSTALLATION.md](../INSTALLATION.md)

## ❓ Проблемы

При возникновении проблем:
1. Проверьте логи в директории `logs/`
2. Убедитесь, что все зависимости установлены
3. См. раздел "Устранение проблем" в [INSTALLATION.md](../INSTALLATION.md)

